# RAVEN Day-5 demonstration

1. Start background research for `FPT` with country `Vietnam`; show the active run, then either cancel it or let it reach identity review. Re-start if cancelled, review the corporate-family choices, and select FPT Corporation.
2. Review complementary research roots, acquire the selected roots, and distinguish approved roots from acquired evidence documents.
3. Inspect qualitative coverage. Strengthen selected gaps or generate the first profile with unsupported fields still unknown; reopen the server-owned candidate before confirmation to show that it is not accepted profile truth.
4. Open a sparse accepted profile, select **Find leadership**, review candidate sources and the patch-only diff, then confirm the new immutable version. Confirm unrelated products and locations remain unchanged in Changes.
5. Open Company List, run **Review workspace**, preview a duplicate merge, archive a sparse test record, restore it, and use permanent deletion only for a disposable record.
6. Open Monitoring to show schedule, manual refresh, and recent runs. Open Investigations to show explicitly saved research artifacts.
7. Open the right-side Ask RAVEN dock. It displays company context and Quick/Deep composer modes. When Hung's backend is connected, demonstrate a grounded answer and explicit Save Investigation; until then, the UI truthfully reports the pending integration and sends no fabricated answer.

The demo must label fixture-backed checks separately from controlled live-provider checks.
