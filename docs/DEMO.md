# RAVEN Day-6 demonstration

1. Enter `FPT` with country `Vietnam`; verify the identity preflight shows the parent first with a bounded, nested family list. Select the intended organization and confirm the existing duplicate check appears before any public-source research.
2. Enter `FPT Software`; verify it resolves directly without a family chooser. Enter `Viettel` to demonstrate family ambiguity, then use **Still can’t find the right organization?** to open guided advice. Minimize and reopen the modal, or choose **Edit search details** to apply the advice in the original form.
3. Try `Sun Property` without enough geography; verify RAVEN asks for a useful hint rather than crawling several countries. Add the country or choose the deliberate exact-name path for an obscure company.
4. Review complementary research roots, acquire the selected roots, and distinguish approved roots from acquired evidence documents.
5. Inspect qualitative coverage. Strengthen selected gaps or generate the first profile with unsupported fields still unknown; reopen the server-owned candidate before confirmation to show that it is not accepted profile truth.
6. Open a sparse accepted profile, select **Find leadership**, review candidate sources and the patch-only diff, then confirm the new immutable version. Confirm unrelated products and locations remain unchanged in Changes.
7. Open Company List, run **Review workspace**, preview a duplicate merge, archive a sparse test record, restore it, and use permanent deletion only for a disposable record. Open Monitoring and Investigations to show their existing workflows.
8. Open the right-side Ask RAVEN dock. It displays company context and Quick/Deep composer modes. When Hung’s backend is connected, demonstrate a grounded answer and explicit Save Investigation; until then, the UI truthfully reports the pending integration and sends no fabricated answer.

The demo must label fixture-backed checks separately from controlled live-provider checks. Identity preflight itself makes no Search or Crawl call before the target is resolved or the user deliberately confirms exact-name research.
